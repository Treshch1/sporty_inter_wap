#name: Run automation tests
#
#on:
#  push:
#    branches:
#      - "develop"
#      - "**"
#
#  workflow_dispatch:
#
#
#jobs:
#  tests:
#    name: Run tests
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v3
#
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v2
#
#      - name: Cache Docker layers
#        uses: actions/cache@v3
#        with:
#          path: /tmp/.buildx-cache
#          key: ${{ runner.os }}-buildx-${{ github.sha }}
#          restore-keys: |
#            ${{ runner.os }}-buildx-
#
#      - name: Build and load
#        uses: docker/build-push-action@v4
#        with:
#          context: .
#          file: ./Dockerfile
#          load: true
#          tags: automation-selenium-tests:latest
#          cache-from: type=local,src=/tmp/.buildx-cache
#          cache-to: type=local,dest=/tmp/.buildx-cache-new,mode=max
#
#        # Temp fix
#        # https://github.com/docker/build-push-action/issues/252
#        # https://github.com/moby/buildkit/issues/1896
#      - name: Move cache
#        run: |
#          rm -rf /tmp/.buildx-cache
#          mv /tmp/.buildx-cache-new /tmp/.buildx-cache
#
#      - name: Configure .env file
#        run: |
#          python -c "import os; file = open('.env', 'w'); file.write(os.environ['TEST_ENVS']); file.close()"
#        shell: bash
#        env:
#          TEST_ENVS : ${{secrets.ENVS}}
#
#      - name: Create folder for failed tests screenshots
#        run: mkdir report_screenshots
#
#      - name: Run tests
#        run: docker compose run --user `id -u` --rm tests
#
#      - name: Archive report
#        if: always()
#        uses: actions/upload-artifact@v3
#        with:
#          name: tests-report
#          path: ./report/
